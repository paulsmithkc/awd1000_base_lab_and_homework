"use strict";

// create the namespace and nested namespaces the application will use
var myapp = myapp || {};
myapp.time = myapp.time || {};
myapp.utility = myapp.utility || {};
myapp.time.clock = myapp.time.clock || {};
myapp.time.stopwatch = myapp.time.stopwatch || {};